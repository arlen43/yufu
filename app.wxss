/* 导入字体图标 */
@import "/assets/style/iconfont.wxss";
/*@import "//at.alicdn.com/t/font_2xmgqg3u7yuba9k9.css";*/
@import "/assets/style/global.wxss";

/*Tab页通用框架*/
.container {
  display: flex;
  flex-direction: column;
}

.top {
  width: 750 rpx;
  overflow: hidden;
  /*background-image: url(https://arlen43.github.io/weassets/images/top.jpg);*/
  color: white;
  padding: 0 20 rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.top-home {
  height: 342 rpx;
}

.top-tab {
  background: #4db718;
}

.top-back {
  position: absolute;
  top: 0;
  left: 0;
  width: 750 rpx;
  height: 342 rpx;
  z-index: -1;
}

.top-left {
  width: 290 rpx;
  /*background: orange;*/
}

.top-right {
  /*background: lightgreen;*/
}

.toolbar {
  height: 90 rpx;
  padding: 20 rpx 0;
  flex: 1 1 auto;

  /*第一次用flex，真是神器啊，跪服！*/
  display: flex;
  /*项目排列的轴向*/
  flex-direction: row;
  /*项目超出一行后如何放置*/
  flex-wrap: nowrap;
  /*flex-flow: row nowrap; 上边两个的缩写，默认为row nowrap*/
  /*项目的水平对齐方式*/
  justify-content: flex-start;
  /*项目的垂直对齐方式*/
  align-items: center;
  /*多根轴线（即超出一行形成多行）的垂直对齐方式，一般不会用到*/
  align-content: flex-start;
}

.toolbar-left {
  /*项目排列顺序，值越小越靠前，默认0*/
  order: 0;
  /*按比例放大项目，0表示不放大，其余值越大，占据比例越大*/
  flex-grow: 0;
  /*按比例缩小项目，0表示不缩小，其余值越大，占据比例越大*/
  flex-shrink: 0;
  /*项目固定占比，如果不想项目放大缩小，设置该值，可与项目宽度设置一致，默认auto，即项目本来大小*/
  flex-basis: 290 rpx;
  /*flex: 0 1 auto; 上边三个的缩写，推荐用该属性。默认值为0 1 auto*/
  /*定制项目的对齐方式，覆盖容器中align-items所指定的*/
  /*align-self: center;*/
}

.toolbar-right {
  flex: 1 1 auto;
  display: flex;
}

.scan {
  display: inline-block;
}

.location {
  margin-right: 0 4 rpx;
  display: inline-block;
}

.search {
  flex: 1 1 auto;
  padding: 2 rpx 5 rpx;
  border-radius: 8 rpx;
  background: #36910b;
  text-align: right;
  height: 100%;
  display: flex;
  flex-direction: row-reverse;
  justify-content: space-between;
  align-items: center;
}

.recommend {
  height: 252 rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 20 rpx 0;
}

.recommend-label {
  flex: 1;
  width: 100%;
  padding-left: 4 rpx;
}

.recommend-main {
  flex: 1;
  display: flex;
  align-items: flex-end;
}

.recommend-left {
  flex: 0 0 290 rpx;
  border-right: 2 rpx solid #fff;
}

.recommend-right {
  margin-left: 20 rpx;
  word-break: break-all;
}

.recommend-food {
  text-align: center;
  font-family: FZYaoTi-M06S;
  letter-spacing: 0 rpx;
}

.recommend-tip {
  padding-left: 4 rpx;
}

.main {
  display: flex;
  flex-direction: column;
}

.fence {
  display: flex;
  flex-direction: column;
  margin-top: 20 rpx;
  background: #f7f7f9;
}

.fence-top {
  display: flex;
  justify-content: space-between;
  height: 70 rpx;
  align-items: center;
  padding: 0 20 rpx;
}

.fence-title {
  border-left: 10 rpx solid #4db718;
  line-height: 40 rpx;
  height: 40 rpx;
}

.fence-more {

}

.fence-sort {
  padding: 0 5 rpx;
  border-right: 1px solid #aaa;
}

.fence-sort:last-child {
  border-right: none;
}

.fence-main {
  display: flex;
  flex-direction: column;
}

.item {
  display: flex;
  height: 220 rpx;
  padding: 10 rpx 20 rpx 10 rpx 20 rpx;
  border-top: 1 rpx solid #a0a0a0;
}

.item-present {
  flex: 0 0 165 rpx;
}

.item-present image {
  width: 210 rpx;
  height: 165 rpx;
}

.item-detail {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  margin-left: 20 rpx;
}

.item-title {
  flex: 0 0 60 rpx;
  height: 60 rpx;
}

.item-desc {
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
  flex: 1;
  width: 360 rpx;
}

.item-author {
  flex: 1;
  display: flex;
  align-items: flex-end;
}

.item-author image {
  width: 40 rpx;
  height: 40 rpx;
  border-radius: 40 rpx;
}

.item-exta {
  flex: 1;
  display: flex;
  align-items: flex-end;
}

.item-exta image {
  width: 170 rpx;
  height: 35 rpx;
}

.item-operate {
  flex: 0 0 100 rpx;
  display: flex;
  flex-direction: column;
}
